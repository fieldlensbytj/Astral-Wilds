# Astral Wilds — Claude Code Instructions

See `AGENTS.md` at the project root for the full shared convention (git workflow, shared context files, project rules). It applies to Claude Code sessions in this repo too. Summary of the part that matters most:

- `git pull` before starting work, `git push` after committing, every session, without asking TJ first.
- If a push is rejected, `git pull` again to merge, resolve conflicts if any, then push. Never force-push.
- Read `Docs/AI/WorkQueue.md` and `Docs/AI/Verification.md` first; leave a dated note under `Docs/AI/` when you finish.
