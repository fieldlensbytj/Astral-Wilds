# Astral Wilds Work Queue

## ENGINE STATUS (2026-09-18): UNREAL IS NOW ACTIVE, UNITY IS FROZEN

TJ confirmed directly on 2026-09-18: **Unreal Engine (`Astral_Wilds_Unreal/Astral_Wilds/`) is the active project going forward. The Unity project below (everything else in this repo) is deprecated/frozen** — left in place for now, not being developed further. Everything below this note is the Unity project's historical queue/status log, kept for reference and design provenance (validated battle math, economy rules, etc.) — not an active task list. See `Docs/AI/CoworkReview-20260918.md` and `Docs/AI/CoworkReview-20260918-AutomationTests.md` for what's known so far about the Unreal side and what still needs a real Unreal-side WorkQueue of its own (a future session should probably split this into `WorkQueueUnreal.md` once there's enough Unreal-specific queue content to warrant it).

Unreal starting point, ported 2026-09-17/18 from the Unity prototype's validated mechanics:
- `AstralCombatRules` (C++, static damage math for basic attack/Arc Burst/Guard).
- `UAstralBattleEngine` (C++, 2v2 battle round resolution: queuing, counterattacks, retargeting, guard reduction) — direct C++ port of `Assets/Scripts/AstralBattleEngine.cs`.
- `AstralMageCharacter` (extends the Unreal Third Person template character; wires Attack/Arc Burst/Guard/Interact-Resonance-Weave via Enhanced Input).
- `AstralResonanceWeaveComponent` (new: a Track→Hold→Harmonize bonding minigame per the Canon Bible; no Unity equivalent existed).
- `AWildAstralEncounter` (new: placeable wild Astral actor with a behavioral-state Temperament, wired into `AstralMageCharacter::DoInteract()` and the Resonance Weave; added 2026-09-18).
- Content/ (the ~137MB template binary assets) is intentionally not committed yet, same reasoning as the Unity project's large art files.
- `Source/Astral_Wilds/Tests/` (new 2026-09-18 afternoon): Unreal Automation Test coverage — `AstralCombatRulesTests.cpp`/`AstralBattleEngineTests.cpp` (the previously-untested pure battle math, cross-checked against real verified Play Mode results already logged below), `AstralWildEncounterTests.cpp` (the receptiveness state machine), `AstralResonanceWeaveComponentTests.cpp` (the bonding minigame's non-Tick public surface). **Build-verified AND runtime-verified**: `Automation RunTests AstralWilds` in-editor → all 19 tests completed with result 'Success'. See `Docs/AI/CoworkReview-20260918-AutomationTests.md`.

---

Checkpoint: 2026-09-17 Asia/Riyadh (UTC+3). The earlier stabilization deadline is complete; this queue now tracks continued full-game development. **(Unity-specific; historical — see engine status note above.)**

Non-negotiable economy constraint: no real-money purchases, paywalls, premium currency, paid progression, or rewarded-ad currency. All purchases use currency earned through gameplay (bosses, item sales, exploration finds, quests, and related play rewards). See `Docs/Design/EconomyPolicy.md`.

## Current priority

1. DONE (2026-09-16 Cowork): Exercise the full keyboard sequence in Play Mode and capture observed results.
2. DONE (2026-09-16 Cowork): Verify persistent save/load across an actual Play Mode exit/re-entry (verified within a single session via forced state change + load; a true Editor exit/re-entry reload was not separately tested).
3. DONE (2026-09-16 Cowork): Verified R as a fainted-slot replacement in battle via a real forced faint (partial HP carried across two encounters, then opponent counter-fire fainted the active slot; R correctly pulled a healthy reserve member into the empty slot).
4. DONE (2026-09-16 Cowork): Built-player smoke test. Also found and fixed a real build-breaking bug in the process (see Status below) -- N restart was not separately re-tested this session (already covered by the two-encounter loop in the prior run).
5. DONE (2026-09-16 Cowork): Replaced keyboard-only shortcuts with a real clickable battle/party UI using the approved creature portraits (see Status below). Keyboard shortcuts still work unchanged alongside it.
6. DONE (2026-09-16 Cowork): Rebuilt the standalone player -- the new UI renders and is fully clickable outside the Editor (verified: clicked Encounter, state advanced correctly).
7. DONE (2026-09-16 Cowork): Encounters now rotate through different wild pairs (verified encounter 1 = Ember/Frost, encounter 2 = Stone/Gale); added a DemoObjectiveComplete banner (beacon activated + 2 encounters cleared) verified live. Encounter trigger itself is still global (B from anywhere), not spatially gated -- a good next step but not done this session.
8. DONE (2026-09-16 Codex): Normalized the first Cindrel production mesh to 35,000 triangles, validated the FBX round trip, imported it into Unity, and created a reusable URP prefab plus build-excluded validation scene. Final texture/retopology/rig/animation remain separate gates.
9. DONE (2026-09-17 Codex): Replaced the global encounter trigger with a spatially gated, visible wild-activity site while preserving both `B` and the clickable HUD entry point. Added deterministic zone tests and verified the full gate-to-battle transition in Play Mode.
10. DONE (2026-09-17 Codex): Made encounter identity zone-driven, added a second distinct site, and added one-time clear state. Ember Hollow exposes Ember/Frost; Stormbreak Grove exposes Stone/Gale. Clearing the first disables its marker and requires travel to the second.
11. DONE (2026-09-17 Codex): Added a persistent HUD objective line that identifies the nearest uncleared site with distance/direction, switches to the second site after the first clear, then guides the player back to the beacon and confirms completion.
12. DONE (2026-09-17 Codex): Activated the existing `Victory` flow as a proper short-demo completion state with Continue Exploring, Save Completion, and protected New Expedition actions. Continuing acknowledges completion and does not reopen the screen every frame.
13. DONE (2026-09-17 Codex): Codified the no-real-money economy policy and added a manifest-level regression test that rejects purchasing and ad-monetization packages.
14. DONE (2026-09-17 Codex): Compiled the legacy IMGUI overlay only into Editor/debug managed variants, modernized runtime object lookups, and verified a real non-development Windows build contains the uGUI HUD but no `OnGUI` method or prototype-title string.
15. DONE (2026-09-17 Codex): Implemented the earned-only Starshard wallet, encounter-clear rewards, a one-time exploration cache, safe spend/item-sale operations, save schema v3, and live HUD balance. No payment or premium-currency path exists.
16. DONE (2026-09-17 Codex): Added a spatial Wayfarer Supply Relay, Salvaged Alloy encounter loot, Field Tonics, atomic buy/sell transactions using earned Starshards only, save schema v4 economy snapshots, and live inventory/HUD controls.
17. DONE (2026-09-17 Codex): Added two persistent, one-time Salvaged Alloy exploration finds and verified the full discover -> inventory -> relay sale loop.
18. DONE (2026-09-17 Codex): Added per-slot Guard actions and a high-pressure Stormbreak battle profile with tested damage reduction and live HUD controls.
19. DONE (2026-09-17 Codex): Added Arc Burst as a two-target tradeoff and explicit per-slot counterattack/Guard round summaries.
20. DONE (2026-09-17 Codex): Added a persistent post-expedition Wayfarer Commission with a one-time, gameplay-earned quest reward and objective-HUD progression.
21. DONE (2026-09-17 Codex): Added a frozen title screen, Continue/New Expedition flow, pause shell, persistent volume/look settings, safe input gating, and deterministic keyboard/controller menu focus; verified in a fresh non-development Windows build.
22. Next production step: add explicit input rebinding/controller prompts and the first audio-feedback/mixer pass without changing character appearance.
23. Next Cindrel art step: author a reference-faithful texture, then produce deformation-ready quad topology, rigging, idle/run/battle animation, and LODs. Character appearance decisions must be reviewed with the user; any paid provider call also requires an explicit per-call credit ceiling.
24. Remaining project operations: Re-enable the "Astral Wilds daily check-in" scheduled task (disabled 2026-09-16 as a precaution, later ruled unnecessary; safe to re-enable).

## Status
- 2026-09-17 (Codex, production shell): Added a real title screen with New Expedition, validated Continue, and an explicit no-real-money statement; added pause/resume/return-to-title without losing live battle state; added automatically persisted five-step master-volume and look-sensitivity settings; and deterministic first-button focus for keyboard/controller UI navigation. The full EditMode suite passed 52/52. Play Mode verified title freeze/input blocking, battle pause/settings/resume preservation, settings application (`0.00` volume / `1.25` sensitivity during the probe), restored preferences, return to title, and Continue loading the existing checkpoint. A fresh Windows x64 non-development build succeeded at 157,132,009 bytes with 0 errors. Mono.Cecil confirmed title/shell literals present and `OnGUI` absent. Build warnings were package shader variants plus the known optional pipeline warning; no first-party script error was introduced. No provider credits were spent.
- 2026-09-17 (Codex, Wayfarer Commission): Added a persistent post-expedition quest that tracks two Salvaged Alloy sales, unlocks after the two-site/beacon Victory, and awards 40 earned Starshards exactly once. Advanced saves to schema v6 with legacy migration and impossible-state validation. The full EditMode suite passed 48/48. Play Mode verified the complete extended route: find and sell two alloy, clear both encounters, restore the beacon, enter Victory, continue, receive exactly +40 quest Starshards (final balance 170), and see the completed commission in the objective HUD. No real-money path or provider spend was added.
- 2026-09-17 (Codex, Arc Burst and round feedback): Added Arc Burst on `F` and the clickable HUD. It deals 8 damage to both living opponents versus Attack's focused 12, creating an explicit focus/total-pressure tradeoff. Counterattack resolution now reports each affected party member, damage, Guard use, and fainting. The full EditMode suite passed 44/44. Play Mode verified both opponents at 30 -> 22, party slots at 30 -> 24 and guarded 30 -> 28, the exact round summary, Battle state retention, and the Arc Burst HUD action. No provider credits were spent.
- 2026-09-17 (Codex, Guard/Stormbreak tactics): Added a per-active-slot Guard action on `G` and the clickable HUD. Guard consumes that Astral's action and reduces its next counterattack to one-third damage rounded up. Ember Hollow remains a 6-damage balanced pair; Stormbreak Grove now uses a 10-damage Stormfront profile with an explicit tactical brief. The full EditMode suite passed 43/43. Play Mode verified the Stormbreak round exactly: unguarded slot 30 -> 20, guarded slot 30 -> 26, Battle state retained, and Guard present in the HUD. No character art or provider credits were touched.
- 2026-09-17 (Codex, exploration salvage loop): Added reusable one-time item pickups and two glowing Salvaged Alloy scatters at `(-5, 0.35, 2)` and `(6, 0.35, -3)`. Advanced saves to schema v5 with stable collected-item IDs. The full EditMode suite passed 36/36. Play Mode verified both finds, automatic disappearance, duplicate-ID rejection, HUD counts, and selling both pieces for 30 earned Starshards. No provider credits were spent.
- 2026-09-17 (Codex, earned-only vendor loop): Added `AstralInventory`, atomic vendor transactions, Salvaged Alloy encounter loot, and Field Tonics that heal the most injured conscious party member. Added the cyan Wayfarer Supply Relay near the crashed beacon, keyboard/clickable controls, live HUD counts, and version-4 economy snapshots with explicit v1-v3 Starshard migration. The expanded EditMode suite passed 34/34. Play Mode verified encounter reward (50 Starshards + 1 alloy), vendor proximity, sale (+15), duplicate-sale rejection without mutation, tonic purchase (-30), tonic healing, and the vendor HUD. Scene was saved cleanly. No real-money path or provider spend was added.
- 2026-09-17 (Codex, earned-currency foundation): Added an `AstralWallet` domain model for gameplay rewards, safe spending, item-sale proceeds, restore/reset, and overflow protection. Encounter-site victories award 50 Starshards; a gold cache at `(-4, 0.7, -5)` awards 25 once. Save schema v3 records balance and collected cache IDs while continuing to accept v1/v2. HUD state shows the live Starshard balance. The expanded EditMode suite passed 27/27. Play Mode verified cache collection, duplicate rejection, marker disappearance, encounter reward (balance 25 -> 75), reward messaging, and HUD `Starshards 75`. Existing persistent-save bytes were not overwritten. Final Console: 0 errors/warnings; scene clean. No provider credits were spent.
- 2026-09-17 (Codex, release UI hardening): Guarded the IMGUI debug overlay behind `UNITY_EDITOR || DEBUG` and replaced seven obsolete ordered object searches with `FindAnyObjectByType`. The 22-test EditMode suite passed. Built a real Windows x64 player with `BuildOptions.None` (157,092,965 bytes); build result succeeded and Development was false. Mono.Cecil inspection of the emitted `AstralWilds.Runtime.dll` confirmed `OnGUI` absent, `ASTRAL WILDS PROTOTYPE` absent, and the uGUI `ObjectiveLine` present. The only final build warning was a package-level missing optional RuntimePipelineConfig; project-script warnings were eliminated. Scene remained active and clean. No provider credits were spent.
- 2026-09-17 (Codex, completion/economy policy pass): The finished demo objective now enters `Victory`, blocks movement, and shows Continue Exploring, Save Completion, and New Expedition actions. New Expedition uses the existing confirmation gate; Continue records acknowledgement and restores free exploration without reopening Victory. Added `Docs/Design/EconomyPolicy.md` as a hard prohibition on real-money systems and a test that fails if purchasing/ad-monetization packages enter the manifest. Completion flow was verified live; the expanded EditMode suite passed 22/22. No provider credits were spent.
- 2026-09-17 (Codex, exploration guidance pass): Expanded the clickable HUD status panel with a cyan objective line driven by live world state. It selects the nearest available site and reports rounded distance plus an eight-way world direction; after two clears it redirects to the crashed beacon; after activation it confirms completion. Direction mapping has focused tests. Full EditMode assembly passed 21/21. A Play Mode sequence verified exact guidance progression (`Ember Hollow: 10 m northwest` -> `Stormbreak Grove: 21 m southeast` -> beacon -> objective complete) and confirmed the runtime `ObjectiveLine` matches controller state. Unity finished clean with 0 Console errors/warnings. No provider credits were spent.
- 2026-09-17 (Codex, multi-site encounter pass): Encounter zones now own stable IDs and wild-pair identity. Added violet Stormbreak Grove at `(9, 0, -7)` alongside Ember Hollow. Victories clear the active site, hide its marker, reject repeat encounters there, and serialize cleared zone IDs in backward-compatible save schema v2 (v1 remains accepted). All 15 EditMode tests passed. A live two-site run verified Ember/Frost identity, victory and marker clear, rejection at the cleared site, travel to Stormbreak Grove, Stone/Gale identity, and entry into the second battle. Existing persistent-save bytes were not overwritten during this probe. Unity returned to a clean scene with 0 Console errors/warnings. No provider credits were spent.
- 2026-09-17 (Codex, spatial encounter pass): Added `AstralEncounterZone`, a visible teal wild-activity site at `(-8, 0, 6)`, controller eligibility gating, and HUD disabled/enabled state. Spawn attempts are rejected with guidance; entering the site enables the unchanged Encounter -> 2v2 Battle flow. All 14 EditMode tests passed; a Play Mode probe verified rejection at spawn, eligibility at the site, Encounter transition, and Battle transition. Unity finished in Edit Mode with `Assets/Astral.unity` clean and 0 Console errors/warnings. No provider credits were spent.
- 2026-09-16 (Codex, Cindrel production pass): Preserved and audited the 975,404-triangle Meshy source; produced a new 35,000-triangle Blender/FBX asset with grounded meter scale, normals, UVs, review material, hashes, and provenance; verified the FBX round trip; imported it through Unity's ModelImporter; created `Cindrel_Production_v01.prefab` with an external URP material and capsule collider; saved a separate validation scene excluded from builds; rendered it in Unity; and restored `Assets/Astral.unity` active and clean. No paid credits were spent. See `Docs/AI/CindrelAssetProductionSpec.md` and `Docs/AI/Verification.md`.
- 2026-09-16 (Cowork, UI run): Built a real clickable battle + party interface (`Assets/Scripts/UI/AstralBattleHUD.cs`), self-installing via `[RuntimeInitializeOnLoadMethod]` so no scene/prefab editing was needed. Cropped 5 approved creature portraits from the reference sheets into `Assets/Resources/Portraits/` with an auto-sprite-import `AssetPostprocessor`. Added a purely additive public Ui*/GetXUiInfo() API to `AstralDemoLoopController` -- every existing private method and the keyboard controls are unchanged. Verified the full loop live via mouse clicks: encounter -> battle (attack, retarget, fainted-tint) -> victory -> recruit -> party management -> exploration, matching the already-verified keyboard-driven rules exactly. See `Docs/AI/Verification.md` for full detail.
- 2026-09-16 (Cowork, fainted-slot + build verification run): Deliberately left a party member at partial HP after one battle (HP persists across encounters), then let a second battle's opponent counter-fire finish fainting the active slot. Pressed R: the fainted slot was correctly replaced by a healthy reserve member (ReplaceFaintedFromReserve -> SwitchToBench(true)), confirmed via Debug Inspector. Then ran a built-player smoke test: created a Windows Build Profile and built to a standalone player. First build FAILED with `Fatal error in Unity CIL Linker: Mono.Cecil.AssemblyResolutionException: Failed to resolve assembly: 'nunit.framework'` -- root cause was that the project had no .asmdef files at all, so the NUnit-based EditMode test file (Assets/Tests/EditMode/AstralBattleFormatTests.cs) was compiling into the default player-included Assembly-CSharp. Fixed by adding `Assets/Scripts/AstralWilds.Runtime.asmdef` (runtime code, included in player builds) and `Assets/Tests/EditMode/AstralWilds.Tests.EditMode.asmdef` (Editor-only, `includePlatforms: ["Editor"]`). That split then broke the test file's access to `AstralReserveCollection.TryAdd` and other `internal` members (CS1061), fixed via `Assets/Scripts/AssemblyInfo.cs` adding `[assembly: InternalsVisibleTo("AstralWilds.Tests.EditMode")]` -- a minimal, non-behavior-changing fix consistent with the "don't fix without a verified repro" convention (the repro was the failed build itself). Rebuilt successfully ("Build completed with a result of 'Succeeded' in 42 seconds"). Launched the built `Astral Wilds.exe` outside the Editor: it started cleanly, rendered the exploration scene (crashed corvette, terrain) matching the Editor's Play Mode view, and displayed the correct debug overlay (Party 5/6, Reserve 0, five Astrals at full HP). Confirmed it responds to real input (mouse-look moved the camera). The player was left running rather than force-closing it via a system-level shortcut; it can be closed manually. See `Docs/AI/CoworkReview-20260916-PlayModeVerified.md` for the fuller narrative of the prior run this builds on.

- Domain party/2v2 rules: Verified by Unity MCP smoke test.
- Reserve switching and duplicate-action guard: Verified by Unity MCP smoke test.
- Playable runtime controller: Implemented and compiled; attached to `Assets/Astral.unity` and scene saved. End-to-end input remains unverified.
- Full end-to-end gameplay: VERIFIED via direct keyboard input in Play Mode (2026-09-16 Cowork run). See CoworkReview-20260916-PlayModeVerified.md.
- Meshy/paid services: no spend authorized or performed in this work window.
- Claude direct diagnostic: READY, exit code 0. The one follow-up JSON review returned empty output with no findings/error payload; it is not treated as a passed review.
- Unity MCP reconnect checkpoint: controller attached, Play Mode entered/exited, Console has 0 errors and 1 render-texture warning from capture.
- 2026-09-16 (Cowork, later run): Attempted the Play Mode keyboard sequence via desktop computer-use control (not MCP). Got Play Mode running and confirmed the exploration scene renders correctly, then lost the Unity Editor process to a window-management mishap and could not relaunch it via Unity Hub's project list this session (double-click on the project name only opens inline rename, no working "open" control found in ~15 attempts). No gameplay sequence evidence gathered; no code changes made. See `Docs/AI/CoworkReview-20260916-PlayModeAttempt.md` for full detail. Next session should re-verify the Editor is actually open before attempting automation, and avoid resizing the OS window.
- 2026-09-16 (Cowork, Play Mode verification run): Full keyboard sequence exercised end-to-end via desktop computer-use control against the Debug Inspector (ground truth, not the tiny on-screen GUI text). Exploration -> B encounter -> Enter battle -> 1/2 slot select, Q/W target, A attack, S voluntary bench swap -> victory -> R recruit (verified both party-add and reserve-overflow paths) -> P party management -> Enter exploration -> K save -> forced state change -> L load correctly reverted to the saved checkpoint. Ran two full encounter/battle/recruit cycles back to back; encountersCompleted incremented 1 -> 2 correctly. Console stayed at 0 errors/0 warnings throughout. No code changes were needed. Not yet covered: R as a fainted-slot replacement in battle (requires 5 rounds of opponent counter-damage to reach naturally; ForceSelectedFaintForPrototypeTesting() exists but isn't wired to a key), N restart, and a built-player test. See `Docs/AI/CoworkReview-20260916-PlayModeVerified.md`. The "Astral Wilds daily check-in" scheduled task was disabled mid-session as a precaution against a suspected (and later ruled out) concurrent-control conflict; re-enable it once confirmed safe.
